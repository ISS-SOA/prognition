!!!
%html
  %head
    %title Codecadet badges information
  %body
    %h1 Badges List
    %table
      %tbody
        %tr
          %th Titles
          %th Dates
        - @badges_found.each do |title, date|
          %tr
            %td #{title}
            %td #{date}
    -#%p #{@username} has collected  #{@badges_found.size} badges.
    -#- @badges_found.each do |title, date|
    -#  Earned #{title} on #{date}.</br>
