%ul{class:'nav nav-tabs'}
  - tabs = {cadet:'Search for a Cadet', tutorials:'Check a Group'}
  - tabs.each do |resource, title|
    - tab_class = current_page?("#{resource}") ? 'active' : ''
    %li{class: "#{tab_class}"}
      %a{href: "/#{resource}"} #{title}
